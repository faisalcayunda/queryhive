# trino is deliberately NOT listed here: it is installed with --no-deps by
# run_local.sh and build_dmg.sh. Its dependency list pins orjson, and orjson's
# build script refuses free-threaded Python outright ("orjson v3.12.0 does not
# support free-threaded Python"), which would block the 3.14t build entirely.
# trino/client.py already falls back to stdlib json when orjson is missing.
# Measured cost of that fallback: 2.8ms vs 1.6ms to decode a 10k-row page,
# against ~19ms to write the same rows, so it is noise. Everything else trino
# needs at runtime is pinned below.
lz4
python-dateutil
pytz
requests>=2.32.4
tzlocal
zstandard

openpyxl>=3.1
xlwt>=1.3
fastapi>=0.115
uvicorn[standard]>=0.30
keyring>=25.0
pywebview>=5.1
